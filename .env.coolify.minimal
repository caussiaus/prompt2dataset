# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# COOLIFY MINIMAL CONFIGURATION
# Only set what Coolify doesn't auto-configure
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ═══════════════════════════════════════════════════
# REQUIRED - Only this one variable!
# ═══════════════════════════════════════════════════
MONGO_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# ═══════════════════════════════════════════════════
# OPTIONAL - Only if needed
# ═══════════════════════════════════════════════════
# HF_TOKEN=your_huggingface_token

# ═══════════════════════════════════════════════════
# That's it! Coolify auto-configures:
# ═══════════════════════════════════════════════════
# - SERVICE_FQDN_* (all your domains)
# - SSL certificates
# - Proxy configuration
# - Network routing
# - Volume mounts to /data
#
# Everything else has smart defaults in docker-compose.yml
